name: Frontend Team Helper

on:
  issues:
    types: [opened]
  pull_request:
    types: [opened, synchronize, reopened, labeled] # 增加 labeled 以確保標籤打上後觸發

permissions:
  contents: read
  issues: write
  pull-requests: write
  repository-projects: write 

jobs:
  label-and-project:
    runs-on: ubuntu-latest
    steps:
      - name: 0. 複製專案程式碼
        uses: actions/checkout@v4

      - name: 1. 路徑自動標籤 (PR)
        if: github.event_name == 'pull_request'
        uses: actions/labeler@v5
        with:
          configuration-path: .github/labeler-config.yml

      - name: 2. 關鍵字自動標籤 (Issue)
        if: github.event_name == 'issues'
        uses: actions/github-script@v7
        with:
          script: |
            const title = context.payload.issue.title.toLowerCase();
            let labels = [];
            if (title.includes('bug')) labels.push('bug');
            if (title.includes('頁面') || title.includes('前端')) labels.push('Frontend page (前端頁面)');
            if (labels.length > 0) {
              github.rest.issues.addLabels({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                labels: labels
              });
            }

      # 🌟 這裡使用你剛剛在 money 倉庫設定好的 Secret
      - name: 3. 自動加入數位財務管理系統
        if: github.event_name == 'pull_request'
        uses: actions/add-to-project@v0.5.0
        with:
          project-url: https://github.com/orgs/4team-mma/projects/1
          github-token: ${{ secrets.MY_NEW_PROJECT_TOKEN }}